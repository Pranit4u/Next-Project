// @ts-ignore
import useSound from 'use-sound'

// const [playIntro, { stop: stopIntro }] = useSound('/intro.mp3');
// const [playBegin, { stop: stopBegin }] = useSound('/play.mp3');
// const [playCorrect, { stop: stopCorrect }] = useSound('/correct.mp3');
// const [playWrong, { stop: stopWrong }] = useSound('/wrong.mp3');
// const [playWait, { stop: stopWait }] = useSound('/wait.mp3');

const stopSound = () => {
  // stopIntro();
  // stopCorrect();
  // stopWrong();
  // stopBegin();
  // stopWait();
}

export const playIntroSound = () => {
  // stopSound();
  // playIntro();
}

export const playBeginSound = () => {
  // stopSound();
  // playBegin();
}

export const playCorrectSound = () => {
  // stopSound();
  // playCorrect();
}

export const playWrongSound = () => {
  // stopSound();
  // playWrong();
}

export const playWaitSound = () => {
  // stopSound();
  // playWait();
}
